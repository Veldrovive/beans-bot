"""
Manages a google calendar for social events.

Uses a service account to access the calendar.
Notifies users of events they have RSVP'd for.
Exposes a shared calendar.
Manages polls for 
Allows for DMs to add events without doing it in the main server. Basically a draft function.
    Although I also want to allow for doing it through the main server.
Can take people's emails through DMs so it knows who to add send invitations to for events.
"""